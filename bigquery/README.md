# Edraak BigQuery SQL

This folder contains simple BigQuery Standard SQL scripts for manually creating the Edraak prototype dataset and tables.


All sample data is synthetic

Note: the Terraform layer in `../infra` may also create the dataset and tables. These SQL files are intentionally kept separate for manual setup, testing, and quick demos.
