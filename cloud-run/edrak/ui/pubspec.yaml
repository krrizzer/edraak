name: edraak
description: Edraak — cross-bank financial seatbelt (Flutter web app).
publish_to: none
version: 0.3.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  url_launcher: ^6.2.5
  fl_chart: ^0.68.0
  intl: ^0.19.0
  google_fonts: ^6.2.1
  web: ^1.1.1

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/icons/
